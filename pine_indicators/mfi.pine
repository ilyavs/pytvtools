//@version=5
indicator(title="Custom MFI", shorttitle="MFI_Custom", format=format.price, precision=2, timeframe="")

length = input.int(14, "Length")

plot(ta.mfi(high, low, close, volume, length), "MFI", color=color.purple)
