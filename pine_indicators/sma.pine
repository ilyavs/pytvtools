//@version=5
indicator(title="Custom SMA", shorttitle="SMA_Custom", format=format.price, precision=2, timeframe="")

length = input.int(20, "Length")
src = input.source(close, "Source")

plot(ta.sma(src, length), "SMA", color=color.blue)
